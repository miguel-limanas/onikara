---
phase: 01-n-cleo-supabase-pap-is-e-governan-a
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - supabase/migrations/20260524031051_initial_rpg_schema.sql
  - supabase/seed.sql
autonomous: true
requirements:
  - UIDB-02
  - ADMIN-02
  - ADMIN-03
  - governanca administrativa
  - RLS
must_haves:
  truths:
    - "D-12: Admin CRUD over catalogs/configs and sensitive player/permission actions are auditable."
    - "D-13/D-14: Catalog/config audit stores full before/after, while player/permission actions use redacted summary payloads."
    - "D-15/D-16: Triggers cover catalog/config CRUD, and RPC functions cover sensitive player/permission/support operations."
    - "D-17: Platform audit logs are readable only by `admin`; `master` never reads platform logs."
    - "D-18/D-19: Gameplay settings are explicit domain tables for progression, mana, combat, rarity and loot."
    - "D-20/D-21: Rule domains support `draft`, `active`, `archived` and expose active data through public views."
  artifacts:
    - supabase/migrations/20260524031051_initial_rpg_schema.sql
    - supabase/seed.sql
  key_links:
    - "Audit trigger functions write into `admin_audit_logs` and RLS restricts log reads to `is_admin()`."
    - "Active rule views read from versioned rule tables and are safe for player/game-engine clients."
---

<objective>
Add versioned gameplay rule tables and administrative audit foundations.

Purpose: This plan makes Onikara configurable and traceable before UI surfaces start mutating game balance.
Output: Explicit rules tables, active views, audit log table, trigger/RPC foundations, and initial seed rows.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-n-cleo-supabase-pap-is-e-governan-a/01-CONTEXT.md
@.planning/phases/01-n-cleo-supabase-pap-is-e-governan-a/01-RESEARCH.md
@.planning/phases/01-n-cleo-supabase-pap-is-e-governan-a/01-01-SUMMARY.md
@supabase/migrations/20260524031051_initial_rpg_schema.sql
@supabase/seed.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create explicit versioned gameplay rule tables</name>
  <files>supabase/migrations/20260524031051_initial_rpg_schema.sql</files>
  <read_first>supabase/migrations/20260524031051_initial_rpg_schema.sql, .planning/phases/01-n-cleo-supabase-pap-is-e-governan-a/01-CONTEXT.md</read_first>
  <action>Add explicit domain tables for `progression_rules`, `mana_rules`, `combat_rules`, `rarity_rules`, and `loot_rules`. Each table must include an `id uuid primary key default gen_random_uuid()`, a human-readable `name`, `version integer not null`, `status text not null check (status in ('draft', 'active', 'archived'))`, domain-specific JSONB/rule columns, `created_at`, and `updated_at`. Add constraints/indexes so active lookup is fast and so each domain has a clear single active version strategy. Do not replace these with a generic `platform_settings(key, value)` table.</action>
  <verify>rg -n "progression_rules|mana_rules|combat_rules|rarity_rules|loot_rules|draft|active|archived" supabase/migrations/20260524031051_initial_rpg_schema.sql</verify>
  <acceptance_criteria>
    - All five rule tables exist by exact table name.
    - Each table has a `status` check containing `draft`, `active`, and `archived`.
    - Each table has `version`, `created_at`, and `updated_at`.
    - No generic-only settings model replaces the explicit rule tables.
  </acceptance_criteria>
  <done>Versioned gameplay rule tables are present.</done>
</task>

<task type="auto">
  <name>Task 2: Add active-rule views and RLS policies</name>
  <files>supabase/migrations/20260524031051_initial_rpg_schema.sql</files>
  <read_first>supabase/migrations/20260524031051_initial_rpg_schema.sql</read_first>
  <action>Enable RLS on the five rule tables. Add admin manage policies using `public.is_admin()`. Add public read policies only for active rows or expose active data through views named `active_progression_rules`, `active_mana_rules`, `active_combat_rules`, `active_rarity_rules`, and `active_loot_rules`. The player/game engine should be able to read active views without knowing draft/archive internals.</action>
  <verify>rg -n "active_progression_rules|active_mana_rules|active_combat_rules|active_rarity_rules|active_loot_rules|enable row level security" supabase/migrations/20260524031051_initial_rpg_schema.sql</verify>
  <acceptance_criteria>
    - All five active views exist by exact name.
    - Rule tables have RLS enabled.
    - Admin manage policies use `public.is_admin()`.
    - Public/player access does not expose draft or archived rule versions.
  </acceptance_criteria>
  <done>Active rule consumption is clean and public-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Create admin audit log table and trigger function</name>
  <files>supabase/migrations/20260524031051_initial_rpg_schema.sql</files>
  <read_first>supabase/migrations/20260524031051_initial_rpg_schema.sql, .planning/phases/01-n-cleo-supabase-pap-is-e-governan-a/01-CONTEXT.md</read_first>
  <action>Create `public.admin_audit_logs` with `id uuid`, `actor_user_id uuid`, `actor_role text`, `action text`, `entity_table text`, `entity_id text`, `before_data jsonb`, `after_data jsonb`, `redacted boolean not null default false`, `metadata jsonb not null default '{}'::jsonb`, and `created_at timestamptz`. Enable RLS so only `public.is_admin()` can select audit logs. Add a reusable trigger function for full before/after audit on catalog/config tables. Attach triggers to catalog tables and the five new rule tables. Ensure trigger rows can indicate admin actor via `auth.uid()` and `public.current_app_role()`.</action>
  <verify>rg -n "admin_audit_logs|before_data|after_data|redacted|audit" supabase/migrations/20260524031051_initial_rpg_schema.sql</verify>
  <acceptance_criteria>
    - `admin_audit_logs` exists with before/after and redaction fields.
    - Only admin can read audit logs through RLS.
    - A trigger function writes full before/after for catalog/config rows.
    - Audit triggers are attached to catalog tables and rule tables.
  </acceptance_criteria>
  <done>Administrative CRUD is auditable.</done>
</task>

<task type="auto">
  <name>Task 4: Add RPC foundations for sensitive administrative actions</name>
  <files>supabase/migrations/20260524031051_initial_rpg_schema.sql</files>
  <read_first>supabase/migrations/20260524031051_initial_rpg_schema.sql</read_first>
  <action>Add narrowly scoped administrative RPC foundations for sensitive operations that need redacted audit payloads. Include at least a role/permission audit function such as `public.log_admin_permission_action(target_user_id uuid, action text, metadata jsonb default '{}'::jsonb)` and a generic support action logger such as `public.log_admin_support_action(target_profile_id uuid, action text, summary jsonb default '{}'::jsonb)`. Both functions must check `public.is_admin()` and raise an exception if false. They should insert `admin_audit_logs` rows with `redacted = true` and no full player state dump. Do not grant `master` access.</action>
  <verify>rg -n "log_admin_permission_action|log_admin_support_action|redacted = true|raise exception" supabase/migrations/20260524031051_initial_rpg_schema.sql</verify>
  <acceptance_criteria>
    - Permission/support RPC foundations exist.
    - RPCs check `public.is_admin()` before logging.
    - RPCs write redacted audit rows.
    - RPCs do not expose service-role-only behavior to browser clients.
  </acceptance_criteria>
  <done>Sensitive admin actions have auditable RPC foundations.</done>
</task>

<task type="auto">
  <name>Task 5: Seed initial active rules</name>
  <files>supabase/seed.sql</files>
  <read_first>supabase/seed.sql, .planning/phases/01-n-cleo-supabase-pap-is-e-governan-a/01-CONTEXT.md</read_first>
  <action>Add deterministic seed rows for each explicit rule table: one active progression rule, one active mana rule, one active combat rule, one active rarity rule, and one active loot rule. Use domain-appropriate JSON shapes that match the MVP mechanics already in REQUIREMENTS.md: XP/level scaling, combat vs out-of-combat mana regeneration, initiative/damage defaults, rarity weights, and initial loot rules. Use `on conflict` or stable identifiers if the chosen schema supports it so repeated resets remain reproducible.</action>
  <verify>rg -n "progression_rules|mana_rules|combat_rules|rarity_rules|loot_rules" supabase/seed.sql</verify>
  <acceptance_criteria>
    - `supabase/seed.sql` inserts one active row for each rule domain.
    - Seed data is deterministic and suitable for `supabase db reset`.
    - Rule shapes correspond to the explicit tables/views.
  </acceptance_criteria>
  <done>Local reset includes active gameplay rules.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "active_progression_rules|active_mana_rules|active_combat_rules|active_rarity_rules|active_loot_rules" supabase/migrations/20260524031051_initial_rpg_schema.sql` finds all views.
- [ ] `rg -n "admin_audit_logs|log_admin_permission_action|log_admin_support_action" supabase/migrations/20260524031051_initial_rpg_schema.sql` finds audit foundations.
- [ ] `rg -n "progression_rules|mana_rules|combat_rules|rarity_rules|loot_rules" supabase/seed.sql` finds seeded active rules.
- [ ] `npm run supabase:db:reset` passes if Supabase local stack is running; otherwise record unavailability and verify SQL structurally.
</verification>

<success_criteria>

- All five explicit rules tables exist.
- Active views exist and hide draft/archive versions.
- Audit logs are admin-readable only.
- Catalog/config CRUD has full before/after audit.
- Sensitive support/permission actions use redacted RPC logging.
- Initial active rules are seeded reproducibly.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-n-cleo-supabase-pap-is-e-governan-a/01-02-SUMMARY.md`
</output>
