---
phase: 01-n-cleo-supabase-pap-is-e-governan-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260524031051_initial_rpg_schema.sql
  - supabase/seed.sql
autonomous: true
requirements:
  - UIDB-02
  - ADMIN-01
  - governanca administrativa
  - autenticacao
  - RLS
user_setup: []
must_haves:
  truths:
    - "D-01/D-02: `player` and `admin` are active roles, and `admin` is modeled as platform administrator authority, not a limited operator."
    - "D-03/D-11: `master` is recognized as a claim/role concept but has no special active Phase 1 policies beyond player permissions."
    - "D-04: RLS helper functions read signed Supabase Auth JWT claims, not frontend-only state."
    - "D-05: Initial local admin setup is reproducible through migration/seed, with no client promotion flow."
    - "D-06/D-08: Catalog tables remain publicly readable and gain admin-only write policies through helper SQL such as `is_admin()`."
    - "D-07/D-09/D-10: Admin can operate platform data through RLS, service role is not required by client contracts, and player-owned rows remain scoped to `auth.uid()`."
  artifacts:
    - supabase/migrations/20260524031051_initial_rpg_schema.sql
    - supabase/seed.sql
  key_links:
    - "Catalog write policies call the same admin helper that future `/admin` Supabase JS calls rely on."
    - "Player data policies continue to join through `profiles.user_id = auth.uid()`."
---

<objective>
Create the Supabase role and RLS foundation for Onikara.

Purpose: This plan establishes the security boundary that every admin/player/master surface will depend on.
Output: Claim-aware SQL helpers, role constraints/seed setup, catalog admin write policies, and preserved player privacy policies.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-n-cleo-supabase-pap-is-e-governan-a/01-CONTEXT.md
@.planning/phases/01-n-cleo-supabase-pap-is-e-governan-a/01-RESEARCH.md
@supabase/migrations/20260524031051_initial_rpg_schema.sql
@supabase/seed.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add role claim helper functions</name>
  <files>supabase/migrations/20260524031051_initial_rpg_schema.sql</files>
  <read_first>supabase/migrations/20260524031051_initial_rpg_schema.sql, .planning/phases/01-n-cleo-supabase-pap-is-e-governan-a/01-CONTEXT.md</read_first>
  <action>Add SQL helpers near the existing `set_updated_at()` function. Implement a compact role API based on JWT claims: `public.current_app_role()` returns `coalesce(auth.jwt() ->> 'app_role', 'player')`; `public.is_admin()` returns true only for `admin`; `public.is_master()` returns true only for `master`; and `public.has_app_role(expected_role text)` compares against `current_app_role()`. Keep helpers `stable` and avoid querying application tables from these helpers to prevent RLS recursion. Include a check-friendly comment that `master` is recognized but intentionally grants no extra Phase 1 policies.</action>
  <verify>rg -n "current_app_role|is_admin|is_master|has_app_role|app_role" supabase/migrations/20260524031051_initial_rpg_schema.sql</verify>
  <acceptance_criteria>
    - `supabase/migrations/20260524031051_initial_rpg_schema.sql` contains `create or replace function public.current_app_role()`.
    - `public.current_app_role()` reads `auth.jwt() ->> 'app_role'`.
    - `public.is_admin()` and `public.is_master()` exist.
    - No helper function reads a role table protected by app RLS.
  </acceptance_criteria>
  <done>Role helper functions exist and can be referenced by later RLS policies.</done>
</task>

<task type="auto">
  <name>Task 2: Add admin write policies for catalogs</name>
  <files>supabase/migrations/20260524031051_initial_rpg_schema.sql</files>
  <read_first>supabase/migrations/20260524031051_initial_rpg_schema.sql</read_first>
  <action>For every catalog/configuration table that currently has public select policies (`rpg_classes`, `rpg_races`, `elements`, `status_effects`, `spells`, `class_spells`, `items`, `enemies`, `enemy_spells`, `quests`, `quest_steps`), add explicit admin-only `insert`, `update`, and `delete` policies using `public.is_admin()` in `using` and/or `with check` as appropriate. Keep existing public select policies intact. Do not grant `master` catalog write access.</action>
  <verify>rg -n "admin.*(insert|update|delete)|is_admin\\(\\)" supabase/migrations/20260524031051_initial_rpg_schema.sql</verify>
  <acceptance_criteria>
    - Each catalog table listed above has an admin write policy.
    - Existing `catalogs are readable by everyone` select policies still exist.
    - No policy uses `service_role` or client-side assumptions.
    - No policy grants `master` special catalog privileges.
  </acceptance_criteria>
  <done>Catalogs remain publicly readable and become admin-writable through RLS.</done>
</task>

<task type="auto">
  <name>Task 3: Add admin read/operate policies for player-owned operational data</name>
  <files>supabase/migrations/20260524031051_initial_rpg_schema.sql</files>
  <read_first>supabase/migrations/20260524031051_initial_rpg_schema.sql, .planning/phases/01-n-cleo-supabase-pap-is-e-governan-a/01-CONTEXT.md</read_first>
  <action>Extend policies on player-owned tables (`profiles`, `character_spells`, `inventory`, `equipment`, `character_status_effects`, `character_quests`, `combat_sessions`, `combat_participants`, `combat_events`) so `public.is_admin()` can read and operate platform data. Preserve all existing player-owned policies based on `auth.uid()`. Use explicit policy names such as `admins can read all profiles` and `admins can manage all inventory`. Do not grant `master` access beyond the existing player-owned policies.</action>
  <verify>rg -n "admins can .*profiles|admins can .*inventory|admins can .*combat|is_admin\\(\\)" supabase/migrations/20260524031051_initial_rpg_schema.sql</verify>
  <acceptance_criteria>
    - Admin policies exist for all player-owned operational tables named in the action.
    - Existing `users can ... own ...` policies remain present.
    - `master` is not granted special player-data visibility.
  </acceptance_criteria>
  <done>Platform admin can inspect/operate player state while players remain scoped to their own rows.</done>
</task>

<task type="auto">
  <name>Task 4: Seed local role expectations without client promotion flow</name>
  <files>supabase/seed.sql</files>
  <read_first>supabase/seed.sql, .planning/phases/01-n-cleo-supabase-pap-is-e-governan-a/01-RESEARCH.md</read_first>
  <action>Add a clearly marked seed section documenting the local admin setup contract. If the implementation chooses a seedable role source table for the auth hook, insert deterministic rows there. If the implementation uses `raw_app_meta_data` for local users instead, document the expected claim key `app_role` and why no browser promotion flow exists. In either case, add SQL comments that Phase 1 activates `player` and `admin`, recognizes `master`, and leaves role promotion to seed/migration or maintenance scripts only.</action>
  <verify>rg -n "app_role|admin|master|promotion|role" supabase/seed.sql</verify>
  <acceptance_criteria>
    - `supabase/seed.sql` documents or seeds local `app_role` expectations.
    - The seed states that there is no client promotion flow in Phase 1.
    - `master` is documented as recognized but not specially authorized yet.
  </acceptance_criteria>
  <done>Local reset has a reproducible role/governance story.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "current_app_role|is_admin|is_master" supabase/migrations/20260524031051_initial_rpg_schema.sql` finds all helpers.
- [ ] `rg -n "catalogs are readable by everyone" supabase/migrations/20260524031051_initial_rpg_schema.sql` still finds public catalog reads.
- [ ] `rg -n "admins can" supabase/migrations/20260524031051_initial_rpg_schema.sql` finds admin policies across catalog and player-owned tables.
- [ ] `npm run supabase:db:reset` passes if the local Supabase stack is running; otherwise record that the stack was unavailable and verify SQL structurally.
</verification>

<success_criteria>

- Claim helper functions are present.
- Catalog admin write policies are present.
- Admin operational policies are present.
- Player-owned policies remain present.
- Master is recognized but has no special active policies.
- Seed/local setup documents role expectations.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-n-cleo-supabase-pap-is-e-governan-a/01-01-SUMMARY.md`
</output>
