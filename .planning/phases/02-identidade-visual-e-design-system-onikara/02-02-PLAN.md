---
phase: 02-identidade-visual-e-design-system-onikara
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - .planning/design-system/tokens.css
  - .planning/design-system/components.css
  - .planning/design-system/preview.html
autonomous: true
requirements:
  - UIDB-01
  - UIDB-05
  - identidade visual
  - acessibilidade
must_haves:
  truths:
    - "D-10: Admin, player and master are visually distinct while sharing Onikara brand tokens."
    - "D-11: Preview represents common portal entry and separate shells/routes for `/admin`, `/play` and future `/master`."
    - "D-12: Admin preview is premium minimal and efficient for CRUD/table/form work."
    - "D-13: Player preview is a registered hunter HUD with character, mission, HP/mana, status and inventory cues."
    - "D-14: Master appears as a full visual mode in this phase."
    - "D-15: Master preview shows campaign, scene, mobs, map/environment and read-only player sheets."
    - "D-16: Master is represented as campaign regent, not platform admin."
    - "D-17: Master preview includes campaign-owned files, scenes, mobs, spaces/scenarios and encounter information."
    - "D-18: Master preview shows player sheets as read-only and does not expose direct character editing."
    - "D-19: Master preview avoids freeform permanent sheet mutation and frames consequences as future campaign mechanics."
    - "D-20: Master preview does not expose global software/backend/catalog/rule/platform-permission/admin-audit controls."
    - "D-21: Master feels powerful within campaign scope without looking like owner of the platform."
    - "D-22: Tokens are implemented in two levels: base visual tokens and semantic aliases."
    - "D-23: Tokens include black/crimson/old-gold/neutrals and semantic aliases for surface, text, danger, hp, mana, rarity, element-fire, focus and disabled equivalents."
    - "D-24: Components use semantic tokens wherever possible."
    - "D-25: Required base components exist in CSS and preview."
    - "D-26: Components include loading, empty where applicable, error, disabled, focus and success states."
    - "D-27: Delivery includes Markdown-backed CSS tokens/components and a static HTML preview."
  artifacts:
    - .planning/design-system/tokens.css
    - .planning/design-system/components.css
    - .planning/design-system/preview.html
  key_links:
    - "components.css uses semantic tokens from tokens.css via CSS custom properties."
    - "preview.html links both tokens.css and components.css."
    - "preview.html includes admin, player and master sections."
---

<objective>
Create executable CSS tokens, component styles and a static preview for the Onikara design system.

Purpose: Turn Phase 2 brand decisions into reusable frontend primitives.
Output: `tokens.css`, `components.css`, and `preview.html`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-identidade-visual-e-design-system-onikara/02-CONTEXT.md
@.planning/phases/02-identidade-visual-e-design-system-onikara/02-RESEARCH.md
@.planning/design-system/BRAND.md
@.planning/design-system/assets/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define two-level CSS tokens</name>
  <files>.planning/design-system/tokens.css</files>
  <read_first>.planning/phases/02-identidade-visual-e-design-system-onikara/02-CONTEXT.md, .planning/design-system/BRAND.md</read_first>
  <action>Create `.planning/design-system/tokens.css`. Define base tokens for black, crimson, old gold, neutral text, spacing, radius, shadow, typography and motion. Define semantic aliases for page/panel surfaces, text, border, focus, disabled, success, warning, danger, hp, mana, rarity common/rare/epic/legendary, element fire/water/ice/shadow, and mode admin/player/master. Include `@media (prefers-reduced-motion: reduce)` that disables or minimizes transitions/animations through token values. Use CSS custom properties only; no build step.</action>
  <verify>rg -n -- \"--oni-black|--oni-crimson|--oni-gold|--color-surface|--color-hp|--color-mana|--color-rarity-legendary|--mode-master|prefers-reduced-motion\" .planning/design-system/tokens.css</verify>
  <acceptance_criteria>
    - tokens.css contains base visual tokens.
    - tokens.css contains semantic aliases for UI, RPG resources, rarities, elements and modes.
    - tokens.css includes reduced motion handling.
  </acceptance_criteria>
  <done>Design tokens are executable CSS and cover brand, UI states and game semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Create component style library</name>
  <files>.planning/design-system/components.css</files>
  <read_first>.planning/design-system/tokens.css, .planning/phases/02-identidade-visual-e-design-system-onikara/02-CONTEXT.md</read_first>
  <action>Create `.planning/design-system/components.css`. Implement classes for: button, input, select, modal, tabs, table, badge, toast, stat bar and command/action toolbar. Use semantic tokens from `tokens.css`, not raw hex values except where defining fallbacks is unavoidable. Include component states: loading, empty where applicable, error, disabled, focus-visible and success. Include mode container styles for `[data-mode=\"admin\"]`, `[data-mode=\"player\"]`, and `[data-mode=\"master\"]`.</action>
  <verify>rg -n \"ok-button|ok-input|ok-select|ok-modal|ok-tabs|ok-table|ok-badge|ok-toast|ok-stat-bar|ok-toolbar|focus-visible|is-loading|is-error|is-success|data-mode=|\\[data-mode=\\\"master\\\"\\]\" .planning/design-system/components.css</verify>
  <acceptance_criteria>
    - All required components have CSS classes.
    - Component CSS references semantic token names with `var(--...)`.
    - State classes and focus-visible styles exist.
    - Admin/player/master mode containers exist.
  </acceptance_criteria>
  <done>Component CSS exists and is ready for static preview.</done>
</task>

<task type="auto">
  <name>Task 3: Build static admin player master preview</name>
  <files>.planning/design-system/preview.html</files>
  <read_first>.planning/design-system/tokens.css, .planning/design-system/components.css, .planning/design-system/assets/README.md</read_first>
  <action>Create `.planning/design-system/preview.html` as a standalone HTML page linking `tokens.css` and `components.css`. Include header with Onikara brand and official logo asset. Include sections for all base components and three mode previews: admin premium minimal panel, player registered hunter HUD, and master campaign-regent console. The master section must show campaign, scene, mobs, map/environment and read-only player sheet summaries; it must not show platform-admin actions or global settings controls.</action>
  <verify>rg -n \"tokens.css|components.css|Onikara|data-mode=\\\"admin\\\"|data-mode=\\\"player\\\"|data-mode=\\\"master\\\"|Read-only|Campaign|Scene|Mobs|Player sheets|ok-button|ok-table|ok-stat-bar|ok-toolbar\" .planning/design-system/preview.html</verify>
  <acceptance_criteria>
    - preview.html links tokens.css and components.css.
    - preview.html includes admin, player and master mode sections.
    - preview.html demonstrates all required components.
    - Master preview presents campaign scope and read-only player sheets.
  </acceptance_criteria>
  <done>Static preview validates tokens/components and the three product modes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n -- "--color-surface|--color-hp|--color-mana|--mode-master" .planning/design-system/tokens.css` passes.
- [ ] `rg -n "ok-button|ok-input|ok-select|ok-modal|ok-tabs|ok-table|ok-badge|ok-toast|ok-stat-bar|ok-toolbar" .planning/design-system/components.css` passes.
- [ ] `rg -n "data-mode=\"admin\"|data-mode=\"player\"|data-mode=\"master\"" .planning/design-system/preview.html` passes.
- [ ] Open `.planning/design-system/preview.html` locally and confirm the page renders without missing CSS links.
</verification>

<success_criteria>

- Tokens are two-level and reusable.
- Required components exist with state styling.
- Preview includes admin, player and master as distinct modes.
- Master is represented as campaign regent, not platform admin.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-identidade-visual-e-design-system-onikara/02-02-SUMMARY.md`
</output>
